services:
  - type: web
    name: twitter-mcp-server
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: TWITTER_BEARER_TOKEN
        sync: false
      - key: NODE_ENV
        value: production
