# Required: Twitter/X API Bearer token
TWITTER_BEARER_TOKEN=replace-with-your-token

# Optional: override port (defaults to 3000)
PORT=3000

# Optional: shared secret for protecting /mcp endpoint
MCP_SERVER_API_KEY=change-me

# Optional: override Twitter API base URL (mainly for testing)
TWITTER_API_BASE_URL=https://api.twitter.com/2
